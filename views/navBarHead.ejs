<!DOCTYPE html>
<html>
<head>
    <!-- Include Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <!-- Include Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <style>
        /* Additional styles can be added here if needed */
        .navBar th {
            cursor: pointer;
            padding: 8px 16px;
            border: 1px solid #ccc; /* Default border color */
            background-color: #f8f8f8; /* Default background color */
        }

        button {
            padding: 8px 16px;
            background-color: #333; /* Default button background color */
            color: #fff; /* Default button text color */
            border: none;
            cursor: pointer;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #333; /* Default text color */
            background-color: #f8f8f8; /* same as navbar */
        }
    </style>
</head>
<body>
    <!-- Header with logo, navigation, and cart button -->
    <header class="flex justify-between items-center py-4 px-8 bg-gray-100">
        <!-- Logo -->
        <form action="/" method="get">
            <button class="logo" onclick="submit">Reptiles "r" Us</button>
        </form>

        <!-- Navigation -->
        <table class="navBar">
          <thead>
              <th class="hover:bg-gray-100"><a href="/aboutUs">About</a></th>
              <th class="hover:bg-gray-100"><a href="/services">Our Services</a></th>
              <th class="hover:bg-gray-100"><a href="/contact">Contact</a></th>
          </thead>
      </table>

        <!-- Cart button -->
        <form action="/cart" method="GET">
            <button type="submit" class="hidden"></button> <!-- Hidden button for accessibility -->
            <a href="/cart" class="cart-icon text-gray-700 hover:text-gray-900">
                <i class="fas fa-shopping-cart text-xl"></i> <!-- Font Awesome cart icon -->
            </a>
        </form>
    </header>